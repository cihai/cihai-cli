(cli-reverse)=

(cihai-reverse)=

# cihai reverse

Use `cihai reverse` when you know an English term and want matching CJK
characters. It searches character definitions and prints the matches in a small
YAML record.

## Command

```{eval-rst}
.. argparse::
    :module: cihai_cli.cli
    :func: create_parser
    :prog: cihai
    :path: reverse
```
